# Sajda App — Daily Changes Log
**Date:** September 20, 2026

---

## 1) Quran Audio & Radio — Copyright-Free Replacement ✅

### Quran Tilawat Audio (Copyright-Free)
- Pehle jo audio source tha usko replace kar ke **AlQuran Cloud / islamic.network CDN** par shift kiya — ye recitations open/copyright-free hain (Play Store policy safe).
- **`lib/services/quran_audio_service.dart`** (NEW):
  - Endpoint: `https://api.alquran.cloud/v1/quran/{reciter}` — poora surah-level recitation index
  - Audio files **islamic.network CDN** se stream hoti hain
  - Per-reciter **memory + disk cache** (JSON file per reciter) — offline-friendly + fast repeat loads
  - HTTPS-only URL validation
- **9 Reciters** (`lib/services/quran_service.dart` → `ReciterChoice`):
  - Mishary Alafasy, Mahmoud Al-Husary, Ahmed Al-Ajamy, Ali Al-Hudhaify, Maher Al-Muaiqly, Muhammad Ayyoub, Muhammad Jibreel, Minshawi, Abu Bakr Ash-Shaatree
- Playback **`just_audio` + audio_service** ke zariye (`lib/services/sajda_audio_handler.dart`) — background play, notification controls, ayah-by-ayah seek.

### Quran Radio (Live Stations)
- **`lib/services/radio_service.dart`** (NEW):
  - Source: **MP3Quran.net radio API** — `https://mp3quran.net/api/radio/radio_en.json`
  - Live Icecast streams (24/7 Quran channels), HTTPS-only stations filter
  - Alphabetical sorting + in-memory cache + friendly error handling
- Radio tab home screen navbar mein 2nd position par (`lib/screens/main_shell.dart`)

### Azan / Notification Sound
- Prayer notifications **native `adhan` raw resource sound** use karti hain (`lib/services/prayer_notification_service.dart`) — exact-alarm channel ke saath (CC0 sound, attribution legal screen mein).

---

## 2) Adhkar Multi-Language — Arabic + English Complete ✅

### Kya kiya
- **Official dataset download**: hisnmuslim.com se poori Hisnul Muslim dataset (wahi source jis se app ka bundled data bana hai) download kar ke local files mein save ki:
  - `backend/backend/data/adhkar_source_ar.json` (132 categories, Arabic)
  - `backend/backend/data/adhkar_source_en.json` (132 categories, English)
- **English overlay (official source)**: Bundled items ko Arabic-text matching se official English translations se merge kiya — AI ke bina 269/300 items cover ho gaye.
- **AI fill (Gemini)**: Baqi 31 items (category 1 "جوامع الدعاء" — jo hisnmuslim.com par nahi hai) Gemini se English translate kiye.
- **Urdu / Hindi / Indonesian skip** (user decision): Adhkar mein ye 3 langs empty hain — app gracefully Arabic dikhati hai. (Ayat of the Day mein ye 3 langs official AlQuran Cloud editions se already mojood hain.)

### Final Result
| Language | Coverage |
|----------|----------|
| Arabic   | 300/300 items, 133/133 categories (bundled) |
| English  | **300/300 items, 133/133 categories — FULL** (269 official + 31 AI) |
| ur/hi/id | Skipped for Adhkar (fallback = Arabic) |

- API verified locally (port 5010): `GET /api/adhkar` → 133 categories with translations ✅

### Naye / Updated Files
- **NEW** `backend/backend/scripts/downloadAdhkarSources.js` — official dataset downloader (tolerant JSON parse — en/126.json upstream bug fix, retry logic, resumable)
- `backend/backend/scripts/translateContent.js`:
  - Official English overlay step (normalized Arabic text matching + **global text index** across all 132 source categories)
  - **Positional doc-item resolution** — bundled data mein kuch categories ke sare items ka duplicate `id: 17` hai, is bug se matching toot rahi thi (fixed)
  - `--langs=en,ur,...` CLI option (dynamic AI prompt — sirf requested languages translate hoti hain)
  - Gemini model → `gemini-flash-lite-latest` (higher free-tier quota; `GEMINI_MODEL` env se override)
  - `maxOutputTokens: 8192` + "no content" response ab retriable
  - `ensureDoc()` helper refactor
- `backend/.env` — `GEMINI_API_KEY` (Google AI Studio key, `x-goog-api-key` header ke zariye hi kaam karti hai)

### Run Commands (reference)
```bash
node backend/scripts/downloadAdhkarSources.js                    # dataset download (local cache)
node backend/scripts/translateContent.js --only=adhkar --langs=en # English fill (overlay + AI)
```

---

## 3) Light Mode Theme Fix + Theme Switcher (Flutter) ✅

### Naya Light Palette
| Item | Old | New |
|------|-----|-----|
| Background | `#F8FFFE` (stark) | **`#F5F7FA`** (soft off-white) |
| Card | white, no shadow | **`#FFFFFF` + subtle shadow** (elevation 1, `#121E293B`) |
| Primary text | `#1A1A2E` | **`#1E293B`** (deep charcoal — pure black removed) |
| Secondary text | `#6B7280` | `#64748B` |
| Muted text | `#9CA3AF` | `#94A3B8` |
| Border / Divider | — | `#E5E9F0` / `#F1F4F8` |
| Brand accent (light) | teal `#00BFA5` | **Soft Emerald `#0D5C46`** |

- Dark mode pehle jaisa vivid teal accent use karta hai — `AppColors.accent(isDark)` adaptive helper dono ko handle karta hai.
- Buttons, chips, tabs, navbar indicator, switches, sliders, progress bars, focused borders — sab theme-driven ab adaptive accent use karte hain.
- Shared widgets bhi adaptive: `HeroPanel` (gradient/shadow), `SectionHeader`, `FilterPill`, `ProgressRing`, `EmptyView`, `showAppSnack`.

### Theme Switcher (Light / Dark / System)
- **`lib/state/app_state.dart`**:
  - Naya `ThemeMode` preference (`light` / `dark` / `system`), persist key `sajda_theme_mode`
  - `WidgetsBindingObserver` — System mode mein OS brightness flip live detect hota hai
  - `darkMode` getter ab *effective* brightness batata hai (system-aware — Streak widgets sahi colored rehte hain)
  - Purane users ki migration: purana dark-mode toggle preserved, `sajda_dark` bool sync rakhi gayi
- **`lib/main.dart`**:
  - Dono `MaterialApp`s par `themeMode: state.themeMode`
  - Status bar / nav bar icons ab theme ke saath adaptive (`AnnotatedRegion<SystemUiOverlayStyle>`)
- **`lib/screens/settings_screen.dart`**:
  - Appearance section: purana Dark Mode switch → **3-option selector: Light | Dark | System**
  - Naya `_ThemeOption` widget (emerald highlight, Urdu labels: لائٹ / ڈارک / سسٹم)

---

## 4) Verification
- `flutter analyze` → **No issues found** ✅
- `flutter test` → **52/52 tests passed** ✅
- Backend API local check (port 5010) → `/api/adhkar` 133 categories with English translations ✅

## Pending (User Side)
- [ ] **Backend Railway redeploy** — naye `/api/adhkar` + `/api/ayat` endpoints aur adhkar translations live karne ke liye
- [ ] Privacy policy site (temp_policy) Railway deploy — updated policy live karne ke liye
- [ ] Flutter release build (user khud banata hai)
